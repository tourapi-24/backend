spring:
  application:
    name: backend
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
jwt:
  expiration: 3600000 # 1 hour
  temporary-expiration: 300000 # 5 minutes
